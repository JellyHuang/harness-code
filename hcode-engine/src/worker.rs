//! Worker placeholder.

/// Worker for executing tasks.
pub struct Worker;

impl Worker {
    pub fn new() -> Self {
        Self
    }
}

impl Default for Worker {
    fn default() -> Self {
        Self::new()
    }
}
