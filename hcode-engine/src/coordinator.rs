//! Coordinator placeholder.

/// Coordinator for orchestrating workers.
pub struct Coordinator;

impl Coordinator {
    pub fn new() -> Self {
        Self
    }
}

impl Default for Coordinator {
    fn default() -> Self {
        Self::new()
    }
}
